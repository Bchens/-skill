以项目为单位的记忆管理。每个项目根目录维护两个 Markdown 文件：

项目重大决定.md
bug修复记忆.md
在写代码或修改代码之前，先识别当前项目根目录，确保两个文件都存在，读取它们，并将当前请求与记录的项目意图进行对比。

如果没有相关记录，按照用户当前意愿执行。

项目根目录选择
选择用户当前活跃项目的根目录，按以下优先级：

环境提供的工作区根目录。
用户明确指定的项目文件夹。
包含 .git、package.json、pyproject.toml、Cargo.toml、go.mod 等标记的最近祖先目录。
当前工作目录。
当用户切换项目时，重新检测根目录并使用该项目的文件。不要跨项目复制或合并记忆。

记忆文件
项目重大决定.md
记录长期项目方向，包括：

全局框架、技术栈、架构、路由、状态管理、存储、部署、工具选择。
项目整体形态、原始意图、目标用户、工作流、数据模型、功能边界。
UI 风格、布局原则、视觉语言、交互模式、产品方向。
用户描述的新想法、功能需求、实现方式、约束和偏好。
任何上下文压缩后不应被后续编码意外推翻的决定。
以简洁的日期条目追加，保留旧记录。

bug修复记忆.md
记录开发中的错误和修复，包括：

开发过程中遇到的错误。
因误解用户意图或项目行为而导致的 bug。
修复后记录的问题。
调试结论、根因和预防措施。
每次修复问题或意识到之前的假设错误时追加条目。

必要工作流
识别当前项目根目录。
确保 项目重大决定.md 和 bug修复记忆.md 存在于该根目录。
在规划或编写代码之前，读取两个文件。
用记录来避免偏离用户原始意图。
如果当前请求与已有记忆冲突，简要提及冲突，并以用户最新明确指令为准。
实施所请求的工作。
修改完成后，如果产生了新的、改变了、澄清了或修正了长期项目知识，追加记忆条目。
自动记录触发条件
以下情况在 项目重大决定.md 中记录：

用户描述了新功能或功能外观。
用户决定或改变了整体 UI 风格、页面结构、工作流或产品方向。
选择了框架、库、架构、存储方式、API 模式或构建/部署方法。
用户给出了新想法、约束、编码方式、命名约定或偏好。
变更影响多个文件、未来扩展性或项目原始意图。
以下情况在 bug修复记忆.md 中记录：

修复了一个 bug。
用户说之前的修改是错误的、误解的、不完整的或偏离了意图。
调试揭示了可能再次发生的根因。
解决了测试失败、运行时错误、UI 损坏、状态缺失、数据不匹配或环境问题。
做了不应重复的错误假设。
条目格式
使用简短的日期条目，保持文件易于浏览。

项目重大决定.md：

## YYYY-MM-DD

- 决定：...
  背景：...
  影响：...
bug修复记忆.md：

## YYYY-MM-DD

- 问题：...
  原因：...
  修复：...
  预防：...
行为准则
不覆盖旧记忆，追加新条目。
不记录琐碎的实现细节。
保持条目简洁、事实性。
将这些文件视为用户的项目记忆，而非通用日志。
对话压缩、继续或恢复工作时，先读取当前项目的两个记忆文件再编码。
